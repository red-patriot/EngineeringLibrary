#include "pch.h"
#include "Geometric\Geometry.h"

Geometry eng::operator+(const Geometry & lh, const Geometry & rh) {
    return Geometry();
}

Geometry eng::operator-(const Geometry & lh, const Geometry & rh) {
    return Geometry();
}
